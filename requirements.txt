#
# MedSync (FastAPI + RAG) production requirements
# - Kept minimal for Vercel deployments (avoid pinning transitive deps)
# - Versions are intentionally omitted unless needed for compatibility
#

# API server
fastapi
uvicorn

# Config / uploads
python-dotenv
python-multipart

# Database (Vitals storage)
supabase

# RAG pipeline
langchain-core
langchain-openai
langchain-text-splitters
langchain-chroma
chromadb

# Document / image processing
pymupdf
pillow
pillow-heif

# Compatibility: several ML/data packages in the ecosystem are not yet stable on NumPy 2.x
numpy<2
