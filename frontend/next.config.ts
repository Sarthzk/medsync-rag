/**
 * Next.js runtime/build configuration.
 * Keep this minimal unless app-level framework behavior needs tuning.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
